/**
 * Color math for the theme contrast suite — now living in src/color.ts
 * (the runtime theme derivation shares it). Re-exported to keep the
 * test-suite import surface stable.
 */
export * from '../src/color.js';
